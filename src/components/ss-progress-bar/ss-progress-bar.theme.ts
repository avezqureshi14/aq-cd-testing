import type { ProgressThemeType } from "@/components/ss-progress-bar/ss-progress-bar.props";

const progressConfig: ProgressThemeType = {
  defaultColor: "var(--color-primary-button-bg)",
};

export default progressConfig;
